# Overview

This package is a language server protocol implementation.

This package is published with Akku(https://akkuscm.org/), which is a language package manager for Scheme. 

This package has been tested with Chez Scheme versions 9.4 and 9.5.

This is for ☭.


# Compare to Geiser

Geiser is an Emacs plugin helping lisp coding. It provides eval, completion, macro expand and many other features. However, these functionalities are not established on the basis of static code analyse. For example, Geiser can only complete top-level binding identifiers and these do not include local bindings.

# Test
> bash test.sh